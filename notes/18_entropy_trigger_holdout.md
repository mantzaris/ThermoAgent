# DOET locked holdout record

No v2 holdout has been generated or run. New scenario and LLM seeds will be
written and checksum-frozen only after validation. This note will record the
outcome-seal boundary, health-only observations during execution, replay gate,
and the exact time at which outcomes are first opened.

Prospective compute-capped composition, subject to the final measured budget
approval by the generator: 144 base panels, preferably 696 method episodes, two
applications, four non-nominal regimes with 16 primary-method seeds each per
application, eight nominal seeds per application, four priority methods on all
panels, five secondary comparators on a common 24-, 16-, or 8-panel subset, five
balanced RL seeds per learned method, LLM seed 9101, a 16-period horizon, and
unseen topology `tri_region_bridge_v2`. The subset size is selected by the
pre-outcome runtime-only ladder `[8101,8106,8111]`, `[8101,8111]`, `[8106]`;
priority panels and five-seed replication are preserved. Health-only monitoring
exposes manifest and published-directory counts, never partial outcomes.
