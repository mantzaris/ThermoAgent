# DOET locked holdout record

No v2 holdout has been generated or run. New scenario and LLM seeds will be
written and checksum-frozen only after validation. This note will record the
outcome-seal boundary, health-only observations during execution, replay gate,
and the exact time at which outcomes are first opened.

Prospective composition, subject to measured budget approval by the generator:
144 base panels, 1,296 method episodes, two applications, four non-nominal
regimes with 16 seeds each per application, eight nominal seeds per application,
nine methods, five balanced RL seeds per learned method, LLM seed 9101, and
unseen topology `tri_region_bridge_v2`. Health-only monitoring exposes manifest
and published-directory counts, never partial outcomes.
