# DOET validation selection

Selected `hysteresis_low`. eligible operating point selected

The selection used the preregistered lexicographic rule in `trigger_selection.json`. The data are validation evidence, not the new locked holdout. Budget-matched control rates were derived here and must not be changed after holdout freeze.
