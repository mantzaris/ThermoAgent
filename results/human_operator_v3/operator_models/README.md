# Simulated operator models

The implemented bounded profiles and their exact parameters are cataloged in
`../tables/operator_profiles.csv`. Runtime state and interventions are stored in
the event ledgers. These are simulated operators, not observations of humans.

