# Protocol artifacts

The executed development protocol is `development_protocol.md`. No validation
or holdout `FROZEN` marker exists because Gate 5 failed. This is a protocol
outcome, not an omitted file. The future-human-study documents are preparation
for a separately approved study and contain no participant evidence.
