# Formal v4 raw evidence

Each subdirectory is a formal development matrix. Every run contains an
`episode.json` and an individually gzip-compressed `events.jsonl.gz` ledger.
All 1,590 formal development/Qwen ledgers replay exactly. Individual artifacts
are below 50 MB; pilot raw trees are separately archived under
`../superseded/raw_archives/`.
